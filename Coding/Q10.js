let string='hello my name is ajay vishwakarma hello'
let arr=Array.from(string.split(' '));
for(let key of arr){
    console.log(key.length);
    // for(let i=)
    // let max=key.sort().length;
    // console.log(max)

}

// for(let i=0;i<arr.length)